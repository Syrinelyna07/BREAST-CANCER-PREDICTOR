# Experiment Tracking

This repository currently uses a lightweight experiment log instead of a dedicated tracker.

## Logged artifacts
- `data_out/baseline_metrics.json`
- `data_out/final_validation_log.txt`
- `data_out/baseline_run.csv`

## Suggested future upgrade
If the project grows, consider one of these:
- Weights & Biases
- MLflow
- a structured CSV log committed with each training change
